os: linux
jobs:
  include:
    - dist: jammy
      virt: vm
      language: erlang
      arch: amd64
      otp_release:
        - "25.0.2"
      script:
        - c(hello)
        - hello:start()
    - dist: jammy
      virt: lxd
      language: erlang
      arch: amd64
      otp_release:
        - "25.0.2"
      script:
        - c(hello)
        - hello:start()
    - dist: bionic
      virt: vm
      language: erlang
      arch: amd64
      otp_release:
        - "25.0.2"
      script:
        - c(hello)
        - hello:start()
    - dist: bionic
      virt: lxd
      language: erlang
      arch: amd64
      otp_release:
        - "25.0.2"
      script:
        - c(hello)
        - hello:start()
    - dist: xenial
      virt: vm
      language: erlang
      arch: amd64
      otp_release:
        - "25.0.2"
      script:
        - c(hello)
        - hello:start()
    - dist: xenial
      virt: lxd
      language: erlang
      arch: amd64
      otp_release:
        - "25.0.2"
      script:
        - c(hello)
        - hello:start()
    - dist: focal
      virt: vm
      language: erlang
      arch: amd64
      otp_release:
        - "25.0.2"
      script:
        - c(hello)
        - hello:start()
    - dist: focal
      virt: lxd
      language: erlang
      arch: amd64
      otp_release:
        - "25.0.2"
      script:
        - c(hello)
        - hello:start()
