os: linux
jobs:
  include:
    - dist: jammy
      virt: vm
      language: erlang
      arch: amd64
      otp_release:
        - "25.0.2"
      script:
        - echo "Erlang language installed !!"
        - erlc hello_world.erl
        - erl -noshell -s hello_world hello -s init stop
    - dist: jammy
      virt: lxd
      language: erlang
      arch: amd64
      otp_release:
        - "25.0.2"
      script:
        - echo "Erlang language installed !!"
        - erlc hello_world.erl
        - erl -noshell -s hello_world hello -s init stop
    - dist: bionic
      virt: vm
      language: erlang
      arch: amd64
      otp_release:
        - "25.0.2"
      script:
        - echo "Erlang language installed !!"
        - erlc hello_world.erl
        - erl -noshell -s hello_world hello -s init stop
    - dist: bionic
      virt: lxd
      language: erlang
      arch: amd64
      otp_release:
        - "25.0.2"
      script:
        - echo "Erlang language installed !!"
        - erlc hello_world.erl
        - erl -noshell -s hello_world hello -s init stop
    - dist: xenial
      virt: vm
      language: erlang
      arch: amd64
      otp_release:
        - "25.0.2"
      script:
        - echo "Erlang language installed !!"
        - erlc hello_world.erl
        - erl -noshell -s hello_world hello -s init stop
    - dist: xenial
      virt: lxd
      language: erlang
      arch: amd64
      otp_release:
        - "25.0.2"
      script:
        - echo "Erlang language installed !!"
        - erlc hello_world.erl
        - erl -noshell -s hello_world hello -s init stop
    - dist: focal
      virt: vm
      language: erlang
      arch: amd64
      otp_release:
        - "25.0.2"
      script:
        - echo "Erlang language installed !!"
        - erlc hello_world.erl
        - erl -noshell -s hello_world hello -s init stop
    - dist: focal
      virt: lxd
      language: erlang
      arch: amd64
      otp_release:
        - "25.0.2"
      script:
        - echo "Erlang language installed !!"
        - erlc hello_world.erl
        - erl -noshell -s hello_world hello -s init stop
